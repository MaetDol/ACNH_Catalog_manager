class Sheet {
  
  constructor( element ) {
    this.sheet = element;
    this.datas = [];
  }
  
  addItem( item ) {
    // 삽입 정렬 이용?
  }

  removeItem( id ) {
    // 삽입 정렬 이용?
  }

  hasItem( id ) {
    // 삽입 정렬 이용?
  }

}

// export { Sheet };